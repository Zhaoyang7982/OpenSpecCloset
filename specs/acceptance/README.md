# 业务验收场景（Acceptance）

本目录存放 **Given–When–Then** 形式的业务验收场景，作为实现与测试阶段的判定依据。

- 场景编号：`S-领域-序号`（例如 `S-ACC-01`）。  
- 需求追溯：在场景中引用 `REQ-*` 或流程 `FLOW-*`、策略 `POL-*`。  
- 完整清单见 [`given-when-then.md`](given-when-then.md)。
